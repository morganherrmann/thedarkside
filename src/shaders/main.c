
#include "list.h"

#include <stdbool.h>
#include <assert.h>

#define CHECK_STATUS(status, status_enum) if ((status) != (status_enum)) return false;

//Use this function for writing test cases for part I of this assignment only.


int main(){

}
